<p id="notice"><%= notice %></p>

プロジェクト名：<%= @project.name %>
製作者：<%= @project.user.try(:name) %>
<%= link_to 'Edit', edit_project_path(@project) if current_user == @project.user %> |
<%= link_to 'Back', projects_path %>

<%= render partial: 'assigns/form', locals: { project: @project } %>

<% if @informations %>
  <% @informations.each do |information| %>
    <div class="siimple-alert siimple-alert--teal"><%= information %></div>
  <% end %>
<% end %>

<%= render partial: 'lines/index', locals: { project: @project, lines: @lines } %>
